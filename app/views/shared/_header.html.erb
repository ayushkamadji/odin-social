<div class="ui fixed menu navbar">
  <div class="item">
    Odin social
  </div>
<% if signed_in? %>
  <%= render 'header/user_menu' %>
<% else %>
  <%= render 'header/sign_in_form' %>
<% end %>
</div>
